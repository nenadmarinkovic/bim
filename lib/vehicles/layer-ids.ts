export const STOPS_SOURCE = "stops";
// The hit target: an invisible circle that never loses a collision, so a
// station stays clickable even when its badge is decluttered away.
export const STOPS_LAYER = "stops-circles";
export const STOPS_BADGE_LAYER = "stops-badges";
