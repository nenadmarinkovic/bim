[Unit]
Description=Daily Bim timetable refresh

[Timer]
OnCalendar=*-*-* 04:00:00
# Catches a run missed while the machine was down.
Persistent=true
RandomizedDelaySec=10min

[Install]
WantedBy=timers.target
